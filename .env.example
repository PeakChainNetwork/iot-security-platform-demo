# REST API origin (required for server-side fetches in app routes)
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

# Optional: separate origin for WebSocket when it differs from REST
# NEXT_PUBLIC_WS_BACKEND_URL=http://localhost:8000
