# REST API origin — PeakSoft's hosted backend (the dashboard reads live data from it).
NEXT_PUBLIC_BACKEND_URL=https://shakily-dullness-backache.ngrok-free.dev

# WebSocket origin for live updates (same host, wss).
NEXT_PUBLIC_WS_BACKEND_URL=wss://shakily-dullness-backache.ngrok-free.dev
