# Project structure

The platform is split into a **Next.js** frontend and a **FastAPI** backend.

## Top-level layout

<div className="grid gap-3 sm:grid-cols-3">
  <div className="rounded-lg border bg-card p-4">
    <div className="font-mono text-sm font-medium text-foreground">www/</div>
    <div className="mt-1 text-sm text-muted-foreground leading-relaxed">Next.js App Router UI — dashboard, devices, and this documentation.</div>
  </div>
  <div className="rounded-lg border bg-card p-4">
    <div className="font-mono text-sm font-medium text-foreground">iot-security-backend/</div>
    <div className="mt-1 text-sm text-muted-foreground leading-relaxed">REST + WebSocket API, ingestion, and pipeline services.</div>
  </div>
  <div className="rounded-lg border bg-card p-4">
    <div className="font-mono text-sm font-medium text-foreground">documents/</div>
    <div className="mt-1 text-sm text-muted-foreground leading-relaxed">Internal engineering docs and roadmap (not shipped by Next.js).</div>
  </div>
</div>

## Frontend (`www/src`)

<div className="space-y-2">
  <div className="flex gap-3 rounded-lg border bg-card p-4">
    <code className="mt-0.5 shrink-0 rounded bg-muted px-1.5 py-0.5 font-mono text-xs">app/(app)/</code>
    <div className="text-sm text-muted-foreground leading-relaxed">Product shell routes wrapped by <code>AppShell</code> (home dashboard, devices).</div>
  </div>
  <div className="flex gap-3 rounded-lg border bg-card p-4">
    <code className="mt-0.5 shrink-0 rounded bg-muted px-1.5 py-0.5 font-mono text-xs">app/docs/</code>
    <div className="text-sm text-muted-foreground leading-relaxed">Documentation area with its own sidebar layout (no global marketing header).</div>
  </div>
  <div className="flex gap-3 rounded-lg border bg-card p-4">
    <code className="mt-0.5 shrink-0 rounded bg-muted px-1.5 py-0.5 font-mono text-xs">components/</code>
    <div className="text-sm text-muted-foreground leading-relaxed">Shared UI — shadcn-based primitives, docs sidebar, API explorer.</div>
  </div>
  <div className="flex gap-3 rounded-lg border bg-card p-4">
    <code className="mt-0.5 shrink-0 rounded bg-muted px-1.5 py-0.5 font-mono text-xs">lib/api.ts</code>
    <div className="text-sm text-muted-foreground leading-relaxed">Typed JSON client used by dashboard pages.</div>
  </div>
</div>

## Backend (`iot-security-backend/app`)

<div className="space-y-2">
  <div className="flex gap-3 rounded-lg border bg-card p-4">
    <code className="mt-0.5 shrink-0 rounded bg-muted px-1.5 py-0.5 font-mono text-xs">main.py</code>
    <div className="text-sm text-muted-foreground leading-relaxed">Primary API entrypoint — mounts routers under <code>/api/v1</code>, CORS, lifespan wiring.</div>
  </div>
  <div className="flex gap-3 rounded-lg border bg-card p-4">
    <code className="mt-0.5 shrink-0 rounded bg-muted px-1.5 py-0.5 font-mono text-xs">api/v1/</code>
    <div className="text-sm text-muted-foreground leading-relaxed">Versioned HTTP routers (<code>devices</code>, <code>alerts</code>, <code>metrics</code>, <code>ws</code>, …).</div>
  </div>
  <div className="flex gap-3 rounded-lg border bg-card p-4">
    <code className="mt-0.5 shrink-0 rounded bg-muted px-1.5 py-0.5 font-mono text-xs">domains/</code>
    <div className="text-sm text-muted-foreground leading-relaxed">Domain services and Pydantic schemas consumed by routers.</div>
  </div>
</div>

<Callout title="Scanner service" variant="default">
Some optional routers (for example network scanning) ship from separate entrypoints such as `scanner_main.py`. The bundled OpenAPI snapshot used by this site focuses on `app.main:app`.
</Callout>
